/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'media.istockphoto.com', 
            'domain-uploadthing.com',
            'utfs.io',
        ],
    },
};

module.exports = nextConfig;
